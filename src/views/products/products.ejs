<html>
<%- include('../partials/head') %>
<body>
    <%- include('../partials/header') %>
    <section class="banner">
        <img src="/images/frutos-12.jpg" alt="Banner de ofertas">

    </section>
<main>
   
    <h1>Rincón de Todos Nuestros Productos</h1>
    <!-- -----------------------------------------Granolas--------------------------------------------- -->

    <div class="secciónX">
        <h3>Granolas Rincon Sano</h3>
        <div class="contenedorProductos">
           
            <% Productos.forEach(productos => { %>
                <% if (productos.idCategoria === 1){ %>
                    <div class="productoX">
                        <div class="productoImagen">
                            <a href="/product/Detail/<%= productos.idProducto%>"><img src="/images/<%= productos.Imagen %>" alt="Granola" class="productImg"></a>
                        </div>
                        <div class="productoInformación">
                            <i class="fas fa-truck"></i>
                            <p class="price"><%= productos.Precio%></p>
                            <p class="discount"><%= productos.Descuento%></p>
                            <h4 class="nombreProducto"><%= productos.Nombre%></h4>
                        </div>
                    </div>
                <% } %>
            <% }); %>  
        </div>       
    </div>
    <div class="secciónX">
        <h3>Orgánico</h3>
        <div class="contenedorProductos">
           
            <% Productos.forEach(productos => { %>
                <% if (productos.idCategoria === 2){ %>
                    <div class="productoX">
                        <div class="productoImagen">
                            <a href="/product/Detail/<%= productos.idProducto%>"><img src="/images/<%= productos.Imagen %>" alt="Granola" class="productImg"></a>
                        </div>
                        <div class="productoInformación">
                            <i class="fas fa-truck"></i>
                            <p class="price"><%= productos.Precio%></p>
                            <p class="discount"><%= productos.Descuento%></p>
                            <h4 class="nombreProducto"><%= productos.Nombre%></h4>
                        </div>
                    </div>
                <% } %>
            <% }); %>  
        </div>       
    </div>
    <div class="secciónX">
        <h3>Libre de Gluten</h3>
        <div class="contenedorProductos">
           
            <% Productos.forEach(productos => { %>
                <% if (productos.idCategoria === 3){ %>
                    <div class="productoX">
                        <div class="productoImagen">
                            <a href="/product/Detail/<%= productos.idProducto%>"><img src="/images/<%= productos.Imagen %>" alt="Granola" class="productImg"></a>
                        </div>
                        <div class="productoInformación">
                            <i class="fas fa-truck"></i>
                            <p class="price"><%= productos.Precio%></p>
                            <p class="discount"><%= productos.Descuento%></p>
                            <h4 class="nombreProducto"><%= productos.Nombre%></h4>
                        </div>
                    </div>
                <% } %>
            <% }); %>  
        </div>       
    </div>
    <div class="secciónX">
        <h3>Cosmética Natural</h3>
        <div class="contenedorProductos">
           
            <% Productos.forEach(productos => { %>
                <% if (productos.idCategoria === 4){ %>
                    <div class="productoX">
                        <div class="productoImagen">
                            <a href="/product/Detail/<%= productos.idProducto%>"><img src="/images/<%= productos.Imagen %>" alt="Granola" class="productImg"></a>
                        </div>
                        <div class="productoInformación">
                            <i class="fas fa-truck"></i>
                            <p class="price"><%= productos.Precio%></p>
                            <p class="discount"><%= productos.Descuento%></p>
                            <h4 class="nombreProducto"><%= productos.Nombre%></h4>
                        </div>
                    </div>
                <% } %>
            <% }); %>  
        </div>       
    </div>


</main>>
<%- include('../partials/footer') %>

    
</body>
</html>